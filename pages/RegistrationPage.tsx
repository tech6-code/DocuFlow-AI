import React from 'react';
import { ProjectPageWrapper } from './ProjectPageWrapper';

export const RegistrationPage: React.FC = () => {
    return <ProjectPageWrapper pageType="projectRegistration" />;
};
